import { Router } from "express";

const routes = Router();
// const upload = multer(uploadConfig);

// routes.post(
//   "/charity_events",
//   upload.array("images"),
//   charityEventController.store
// );


export default routes;
